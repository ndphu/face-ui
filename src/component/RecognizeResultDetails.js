import React from 'react';
import {withStyles} from "@material-ui/core";

const styles = (theme) => ({});

class RecognizeResultDetails extends React.Component {

    render = () => {
        const {classes} = this.props;
        return (
            <div>

            </div>
        )
    }
}


export default withStyles(styles)(RecognizeResultDetails);